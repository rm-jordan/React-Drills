/**
 * SESSION 6 (optional) — Stats route
 * TODO: GET / counts by status
 * Reference: ../../../server/src/routes/stats.ts
 */

import { Router } from "express";

export const statsRouter = Router();

// TODO: statsRouter.get("/")
