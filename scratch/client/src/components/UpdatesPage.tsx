/**
 * SESSION 2–6 — Main page (build incrementally)
 * Session 2: load list, loading/error, selectedId, UpdateList + UpdateDetail (read-only)
 * Session 3: CreateUpdateForm + handleCreated
 * Session 4: EditUpdateForm, delete, handleUpdated / handleDeleted
 * Session 5: SearchAndFilters + useMemo filteredUpdates
 * Session 6: comments, StatsPanel, manager review
 * Reference: ../../../client/src/components/UpdatesPage.tsx
 */

export function UpdatesPage() {
  return (
    <div>
      <p className="muted">TODO: implement UpdatesPage</p>
    </div>
  );
}
