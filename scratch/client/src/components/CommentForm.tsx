/** SESSION 6 — TODO: controlled textarea, createComment, onCreated callback */

interface Props {
  updateId: string;
  onCreated: () => void;
}

export function CommentForm(_props: Props) {
  return <p className="muted">TODO: CommentForm</p>;
}
