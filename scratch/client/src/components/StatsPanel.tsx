/** SESSION 6 — TODO: fetchStats on refreshKey, manager-only in UpdatesPage */

interface Props {
  refreshKey: number;
}

export function StatsPanel(_props: Props) {
  return <p className="muted">TODO: StatsPanel</p>;
}
